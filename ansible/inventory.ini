[ngolacloud_dev]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3

[ngolacloud_dev:vars]
# Versions pinned here so re-runs of `make setup` stay reproducible.
# Update one place, not scattered across roles.
docker_version_pin       = 5:29.5.0-1~ubuntu.24.04~noble
kind_version             = 0.30.0
kubectl_version          = 1.34.1
helm_version             = 3.16.3
kustomize_version        = 5.5.0
k9s_version              = 0.32.7
krew_version             = 0.4.4
stern_version            = 1.32.0

rust_toolchain_version   = stable
sccache_version          = 0.8.2
mold_version             = 2.34.0

# Resource budget — calibrated to the i9-13900H reference workstation
# (20 vCPU / 64 GB RAM). MemoryMax leaves 30 GB for the rest of the
# system; MemoryHigh issues kernel pressure 4 GB before the hard cap so
# workloads have a chance to release before being OOM-killed.
slice_memory_max_gb      = 32
slice_memory_high_gb     = 28
slice_cpu_weight         = 75
slice_tasks_max          = 8192

swap_file_path           = /swapfile
swap_file_size_gb        = 16

# Docker IP pool — 172.30.0.0/16 avoids the common Wi-Fi/corporate
# ranges (192.168.x, 172.16-21.x).
docker_address_pool_base = 172.30.0.0/16
docker_address_pool_size = 24
